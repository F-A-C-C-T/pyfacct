image: python:3.9

stages:
  - sync

sync_to_github:
  stage: sync
  script:
    - git remote set-url github https://$GITHUB_TOKEN@github.com/F-A-C-C-T/pyfacct.git
    - git push github --all
  only:
    - master
